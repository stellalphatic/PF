#include <iostream>
using namespace std;
int main()
{
    int length, width;
    cout << "Enter length the rectangle: ";
    cin >> length;
    cout << "Enter width the rectangle: ";
    cin >> width;
    cout << "The area of rectangle is: " << length * width;
}